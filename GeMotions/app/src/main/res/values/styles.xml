<resources>
    <style name="AppTheme" parent="Theme.Material3.Light">
        <!-- Stili per il tema chiaro -->
    </style>

    <style name="AppTheme.Dark" parent="Theme.Material3.Dark">
        <!-- Stili per il tema scuro -->
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.Material3.Light.NoActionBar">
        <!-- Stili per il tema senza ActionBar -->
    </style>

    <style name="AppTheme.Dark.NoActionBar" parent="Theme.Material3.Dark.NoActionBar">
        <!-- Stili per il tema senza ActionBar -->
    </style>

    <style name="AppTheme.RoundedCornerMaterialButton" parent="Widget.AppCompat.Button.Colored">
        <item name="android:background">@drawable/rounded_shape</item>
    </style>

    <style name="RoundedAlertDialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowBackground">@drawable/dialog_background_rounded</item>
    </style>

    <style name="MyRadioButtonStyle" parent="Widget.AppCompat.CompoundButton.RadioButton">
        <item name="android:buttonTint">@color/radio_button_color_selector</item>
    </style>
</resources>
